# Keep application dependencies (node_modules) out of version control
node_modules

# Keep environment variables out of version control
.env

# Keep PrismaClient out of version control
/src/generated
